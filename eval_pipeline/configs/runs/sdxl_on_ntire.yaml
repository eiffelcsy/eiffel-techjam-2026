# One run = one detector x a set of datasets.
# Everything not named here takes its default -- see configs/defaults.yaml.

run_id: sdxl_on_ntire

detector: configs/detectors/sdxl.yaml

datasets:
  - configs/datasets/ntire_val.yaml

degrade: {}     # the standard sweep: all four levels, all eleven transforms
