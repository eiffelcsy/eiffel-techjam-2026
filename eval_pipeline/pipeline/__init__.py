"""The frequency branch's DCT extraction -- everything else has moved to
load_data/, preprocessing/, eval/, and scripts/ at the repo root.

Temporary: this package shrinks to just `pipeline.freq` for Stage 2, and is
retired entirely in Stage 3 once freq/ moves to freq_branch/.
"""
