__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/

# cache_ms/, cache_ms_val/, checkpoints/, results/, logs/, wandb/ moved to the
# repo root -- see the .gitignore there. Every stage reads them, so they live
# above all of this rather than inside one package.
